import { SchedulePredefinedTerm } from './schedule-predefined-term';

describe('SchedulePredefinedTerm', () => {
  it('should create an instance', () => {
    expect(new SchedulePredefinedTerm()).toBeTruthy();
  });
});
