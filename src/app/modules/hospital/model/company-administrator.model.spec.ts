import { CompanyAdministrator } from './company-administrator.model';

describe('CompanyAdministrator', () => {
  it('should create an instance', () => {
    expect(new CompanyAdministrator()).toBeTruthy();
  });
});
